import { ReactNode } from 'react';

const VelocityWrapper = ({ children }: { children: ReactNode }) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default VelocityWrapper;
